name: Auto Tag

# Cuando se mezclan cambios a main, lee la versión de Cargo.toml,
# crea el tag vX.Y.Z correspondiente (si no existe) y crea la
# release de producción. El push del tag dispara el workflow
# `Release` (.github/workflows/release.yml) que compila y sube
# los artefactos (.deb + instalador Windows).
#
# IMPORTANTE: el push del tag se hace con el secret RELEASE_PAT
# (un Personal Access Token con permiso Contents:write), NO con
# GITHUB_TOKEN. GitHub no dispara workflows desde pushes hechos
# con GITHUB_TOKEN (evita loops), así que con GITHUB_TOKEN la
# release quedaría creada pero `Release` jamás correría y no
# habría .deb ni .exe.
#
# Flujo equivalente al versionado manual por tags de umbral
# (v1.5.5, v1.5.4, ...), pero automatizado.

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: auto-tag-main
  cancel-in-progress: false

jobs:
  tag-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (con tags e historial completo)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
          # PAT con Contents:write para que el push del tag dispare `Release`.
          # Ver el encabezado del archivo: con GITHUB_TOKEN no se dispara.
          token: ${{ secrets.RELEASE_PAT }}

      - name: Verificar secret RELEASE_PAT
        run: |
          if [ -z "${{ secrets.RELEASE_PAT }}" ]; then
            echo "::error::falta el secret RELEASE_PAT (PAT con Contents:write). Créalo en Settings -> Secrets and variables -> Actions."
            exit 1
          fi

      - name: Leer versión de Cargo.toml
        id: version
        run: |
          VERSION="$(grep -m1 '^version *=' Cargo.toml | cut -d'"' -f2)"
          if [ -z "$VERSION" ]; then
            echo "::error::no se pudo leer la versión de Cargo.toml"
            exit 1
          fi
          TAG="v${VERSION}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "Versión detectada: ${VERSION} -> tag ${TAG}"

      - name: Crear tag y release si no existen
        env:
          GH_TOKEN: ${{ secrets.RELEASE_PAT }}
        run: |
          TAG="${{ steps.version.outputs.tag }}"
          git fetch --tags origin

          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "El tag $TAG ya existe, no se crea nada."
          else
            echo "Creando tag $TAG..."
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git tag -a "$TAG" -m "Release $TAG"
            git push origin "$TAG"
          fi

          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "La release $TAG ya existe."
          else
            echo "Creando release de producción $TAG..."
            gh release create "$TAG" \
              --title "$TAG" \
              --generate-notes \
              --target "${GITHUB_SHA}"
          fi
